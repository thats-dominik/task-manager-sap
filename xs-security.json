{
  "xsappname": "task-managing-application",
  "scopes": [
    { "name": "$XSAPPNAME.admin",   "description": "Admin access" },
    { "name": "$XSAPPNAME.giver",   "description": "Task giver access" },
    { "name": "$XSAPPNAME.worker", "description": "Task worker access" }
  ],
  "role-templates": [
    { "name": "Admin",   "description": "Admin role",   "scope-references": ["$XSAPPNAME.admin"] },
    { "name": "Giver",   "description": "Giver role",   "scope-references": ["$XSAPPNAME.giver"] },
    { "name": "Worker", "description": "Worker role",  "scope-references": ["$XSAPPNAME.worker"] }
  ]
}
